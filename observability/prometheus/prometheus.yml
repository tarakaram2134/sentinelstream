global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: "sentinelstream-scorer"
    static_configs:
      - targets: ["scorer:8000"]
